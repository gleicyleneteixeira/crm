<%= content_tag :li, id: dom_id(deal), data: { id: deal.id, account_id: Current.account.id, position: deal.position } , class: 'hover:shadow-sm select-none cursor-pointer' do %>
  <%= turbo_stream_from deal %>
  
  <%# QUADRO: Azul Marinho Profundo %>
  <div class="rounded-lg border-2 color-border-default dark:border-slate-800 color-bg-fill-default dark:bg-[#0f172a] transition-colors duration-200" data-controller='load-lucid-icons'>
    
    <%= link_to account_deal_path(Current.account, deal.id), data: { turbo_frame: :_top }, class: 'flex flex-col gap-2 pt-3 px-4' do %>
      <% unless deal.open? %>
        <div class='w-full flex'>
          <%# STATUS: Vencido ou Ganho %>
          <div class='inline-flex gap-1 items-center px-2 py-0.5 rounded text-[10px] font-black uppercase
            <%= deal.lost? ? 'bg-red-100 text-red-700' : 'bg-green-100 text-green-700' %>'>
              <%= Deal.human_enum_name(:status, deal.status) %>
          </div>
        </div>
      <% end %>
      
      <%# NOME DO NEGÓCIO %>
      <p class="typography-body-900 color-fg-default dark:text-slate-100 font-bold text-[14px] leading-tight">
        <%= deal.name %>
      </p>
      <div class="w-full border-[0.5px] color-border-default dark:border-slate-800"></div>
    <% end %>

    <%# CONTATO %>
    <div class='flex items-center py-2'>
      <%= link_to account_contact_path(Current.account, deal.contact), class: "cursor-pointer peer" , data: { turbo_frame: :drawer } do %>
        <div class='flex items-start pl-4 gap-2 text-[12px]'>
          <i data-lucide="user" class="w-3.5 h-3.5 dark:text-slate-400"></i>
          <p class='typography-body-900 color-fg-soft dark:text-slate-300 hover:color-fg-default dark:hover:text-white <%= dom_id(deal.contact, :stream_full_name) %>'>
            <%= deal.contact.full_name %>
          </p>
        </div>
      <% end %>
    </div>

    <div class='flex items-end justify-between px-4 pb-4'>
      <%# AVATARES DOS USUÁRIOS %>
      <div class='flex grow'>
        <% if deal.users.present? %>
          <div class='flex -space-x-2'>
            <% deal.users.order(id: :desc).limit(2).each do |user| %>
              <div class="w-5 h-5 rounded-full bg-slate-700 border border-slate-900 flex items-center justify-center overflow-hidden">
                <i data-lucide="user" class="w-3 text-white"></i>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>

      <%# BLOCO DE VALOR E TAREFAS (PÍLULAS COLORIDAS) %>
      <div class='flex flex-col items-end gap-1.5'>
        
        <%# VALOR: Azul Claro com Texto Azul Escuro %>
        <% unless deal.total_amount_in_cents.zero? %>
          <div class='bg-blue-50 border border-blue-100 px-3 py-0.5 rounded-full shadow-sm'>
            <span class='text-blue-700 font-black text-[11px]'>
              <%= render 'components/formatters/currency', value: deal.total_amount_in_cents %>
            </span>
          </div>
        <% end %>
        
        <%# TAREFAS/TEMPO: Verde para futuro, Vermelho/Laranja para atrasado %>
        <% if deal.next_event_planned? %>
          <% 
            is_overdue = deal.next_event_planned.due_at < Time.current
            bg_class = is_overdue ? 'bg-red-50 border-red-100' : 'bg-green-50 border-green-100'
            text_class = is_overdue ? 'text-red-700' : 'text-green-700'
          %>
          <div class='inline-flex items-center gap-1 px-3 py-0.5 rounded-full shadow-sm border <%= bg_class %>'>
            <i data-lucide="clock" class="w-3 h-3 <%= text_class %>"></i>
            <span class='font-black text-[11px] <%= text_class %>'>
               <%= render partial: 'accounts/pipelines/deal_event', locals: {event: deal.next_event_planned} %>
            </span>
          </div>
        <% end %>

      </div>
    </div>
  </div>
<% end %>
