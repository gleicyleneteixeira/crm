<% minimal = defined?(minimal) ? minimal : false %>
<div class="relative inline-block text-left" 
     data-controller="task-menu" 
     data-task-menu-update-url-value="<%= account_contact_event_path(event.account, event.contact, event, deal_id: event.deal_id) %>"
     data-task-menu-delete-url-value="<%= account_contact_event_path(event.account, event.contact, event, deal_id: event.deal_id) %>">
  
  <div data-task-menu-target="display" class="flex items-center gap-1">
    <button type="button" 
            class="flex items-center justify-center transition-colors rounded-full p-0.5 hover:bg-slate-100 dark:hover:bg-slate-800"
            data-action="click->task-menu#toggleMenu"
            data-task-menu-target="icon">
      <% if event.done? %>
        <i data-lucide="check" class="w-4 h-4 text-emerald-500"></i>
      <% else %>
        <i data-lucide="clock-4" class="w-4 h-4 <%= event.status_color %>"></i>
      <% end %>
    </button>
    
    <% if defined?(show_date) && show_date %>
      <span class="typography-body-800 text-[11px] <%= event.status_color %>">
        <%= render "components/formatters/datetime", value: event.primary_date, type: "distance" %>
      </span>
    <% end %>
  </div>

  <% if minimal %>
    <!-- Inline Edit Form (Hidden) -->
    <div data-task-menu-target="editForm" class="hidden absolute left-0 z-[100] bg-white dark:!bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-md shadow-xl p-2 w-64">
      <%= form_with model: event, url: account_contact_event_path(event.account, event.contact, event, deal_id: event.deal_id), method: :patch, data: { action: "submit->task-menu#submitEdit" } do |f| %>
        <div class="flex flex-col gap-2">
          <%= f.text_field :title, class: "form-input text-xs h-7 px-2 !bg-white dark:!bg-black !text-slate-900 dark:!text-slate-100", placeholder: "O que precisa ser feito?", data: { task_menu_target: "input" } %>
          <%= f.datetime_local_field :scheduled_at, value: event.scheduled_at&.strftime("%Y-%m-%dT%H:%M"), class: "form-input text-xs h-7 px-2 !bg-white dark:!bg-black !text-slate-900 dark:!text-slate-100" %>
          <div class="flex justify-end gap-1 mt-1">
            <button type="button" data-action="click->task-menu#cancelEdit" class="text-[10px] px-2 py-1 text-slate-500 hover:text-slate-700">Cancelar</button>
            <%= f.submit "Salvar", class: "button-default-primary-xs !py-0.5 !text-[10px]" %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

  <!-- Dropdown Menu -->
  <div data-task-menu-target="menu" 
       class="hidden absolute left-0 mt-1 w-40 origin-top-left rounded-md bg-white dark:!bg-slate-900 shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none z-[999] py-1 text-xs border border-slate-200 dark:border-slate-700" 
       role="menu">
    
    <% if event.done? %>
      <a href="#" class="flex items-center gap-2 px-3 py-1.5 text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors" 
         role="menuitem" 
         data-action="click->task-menu#reopenTask">
        <i data-lucide="rotate-ccw" class="w-3.5 h-3.5 text-sky-500"></i>
        <span>Reabrir Tarefa</span>
      </a>
    <% else %>
      <a href="#" class="flex items-center gap-2 px-3 py-1.5 text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors" 
         role="menuitem" 
         data-action="click->task-menu#completeTask">
        <i data-lucide="check" class="w-3.5 h-3.5 text-emerald-500"></i>
        <span>Concluir</span>
      </a>
    <% end %>

    <% if minimal %>
      <a href="#" class="flex items-center gap-2 px-3 py-1.5 text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors" 
         role="menuitem" 
         data-action="click->task-menu#showEdit">
        <i data-lucide="pencil" class="w-3.5 h-3.5 text-sky-500"></i>
        <span>Editar</span>
      </a>
    <% else %>
      <%= link_to edit_account_contact_event_path(event.account, event.contact, event, deal_id: event.deal_id), 
          class: "flex items-center gap-2 px-3 py-1.5 text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors",
          role: "menuitem",
          data: { turbo_frame: dom_id(event) } do %>
        <i data-lucide="pencil" class="w-3.5 h-3.5 text-sky-500"></i>
        <span>Editar</span>
      <% end %>
    <% end %>

    <div class="my-1 border-t border-slate-100 dark:border-slate-800"></div>

    <a href="#" class="flex items-center gap-2 px-3 py-1.5 text-red-600 hover:bg-red-50 dark:hover:bg-red-900/20 transition-colors" 
       role="menuitem" 
       data-action="click->task-menu#deleteTask">
      <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
      <span>Excluir</span>
    </a>
  </div>
</div>

